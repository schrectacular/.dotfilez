- defaults:
        link:
                create: true
                relink: true

- clean: ['~']

- shell:
        # update submodules this needs to be done before links
        - git submodule update --init --recursive

- link:
        ~/.vimrc: vimrc
        # zprezto needs to live here
        ~/.zprezto: zprezto
        # zprezto setup does this programatically, but sets up these links
        ~/.zlogin: zprezto/runcoms/zlogin
        ~/.zlogout: zprezto/runcoms/zlogout
        ~/.zprofile: zprezto/runcoms/zprofile
        ~/.zshenv: zprezto/runcoms/zshenv
        ~/.zshrc: zprezto/runcoms/zshrc
        # I modified these meself
        ~/.zpreztorc: zpreztorc

        #this is for vim:
        ~/.vim/bundle/Vundle.vim: Vundle.vim

# Homebrew stuff
- tap:
    - caskroom/fonts

- brew: [zsh, cask, tmux]

- cask: [macvim, iterm2]

- shell:
        - |
          if [[ $(uname -a) =~ "Darwin" ]]; then
              #run osx things here
              echo "placeholder";
          fi
        - |
          if [[ $(uname) == "Linux" ]]; then
            #run linux things here
            if [[ $(which apt | tail -c 5) == "/apt" ]]; then
                [[ $(which tmux | tail -c 6) == "/tmux" ]] || sudo apt install tmux
                # Vim plugin taglist depends on ctags
                [[ $(which ctags | tail -c 7) == "/ctags" ]] || sudo apt install tmux
            fi
          fi
        # if zsh isn't default shell but it is found, then make it default. Asks for password
        - |
          if [[ ! $SHELL == "/bin/zsh" ]] && [[ $(which zsh | tail -c 5) == "/zsh" ]]; then 
            chsh -s /bin/zsh
          fi
        # vimrc uses this file to store recent regular expressions
        - touch ~/.regexlist.vim
        # update Vundle
        - vim +PluginInstall +qall
